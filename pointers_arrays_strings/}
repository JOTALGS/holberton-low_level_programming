#include "main.h"
#include <stdio.h>
#include <string.h>

void
print_rev(char *s)
{
	int length = strlen(s);
	char str;

	printf("len %d", length);
	printf("%s", s);
	str = s;
	printf("\n %s", str);
}
