#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _isupper - upers
 * @c: int int
 * Return: 0 o 1 qcyo
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
